const Header = (props) => (
  <header>
    <p>Header</p>
  </header>
);

const NavMenu = (props) => (
  <nav>
    <button>Strona główna</button>
    <button>Moje projekty</button>
    <button>Kontakt</button>
  </nav>
);

export default Header;
